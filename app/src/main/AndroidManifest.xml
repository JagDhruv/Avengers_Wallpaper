<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wallpapers.dhruv.jagrit.avengers.avengers" >

    <!-- Include required permissions for Google Mobile Ads to run-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/age"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!--This meta-data tag is required to use Google Play Services.-->
        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <activity
            android:name="wallpapers.dhruv.jagrit.avengers.avengers.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="wallpapers.dhruv.jagrit.avengers.avengers.NextActivity"
            android:label="@string/app_name" >
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value="wallpapers.dhruv.jagrit.avengers.avengers.MainActivity" ></meta-data>
        </activity>
        <!--Include the AdActivity configChanges and theme. -->
        <activity android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
        <activity android:label="@string/app_name" android:name=".FullImageActivity"/>
        <activity android:label="@string/app_name" android:name=".ThorActivity"/>
        <activity android:label="@string/app_name" android:name=".CaptainActivity"/>
        <activity android:label="@string/app_name" android:name=".HulkActivity"/>
        <activity android:label="@string/app_name" android:name=".WidowActivity"/>
        <activity android:label="@string/app_name" android:name=".HawkActivity"/>
        <activity android:label="@string/app_name" android:name=".IronActivity"/>
    </application>

</manifest>
